Things to do better:

- Resources: not all resources have write capabilities
- Images: wrap better base64 code
- References: no-sense names
- Rewrite and improve compilable text in order to make it more readable
- Better text rule system
- Better management of compilation

- Remove Codex from `compile`
- Use different `compile` for all components with required fields of compilation configuration overlay 
- Make `compile` &self from &mut self
- Remove fast compile and use a "fast" variable (enum)
- Assemble take dossier/document